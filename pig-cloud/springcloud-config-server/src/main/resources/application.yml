server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/han895268684long/config.git

logging:
  config: classpath:logback.xml


